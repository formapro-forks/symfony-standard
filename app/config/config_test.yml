imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

enqueue:
    transport:
        default: 'null'
        'null': true
    client:
        traceable_producer: true
